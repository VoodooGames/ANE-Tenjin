<?xml version="1.0" encoding="UTF-8"?>
<project name="ANE test" default="install.ios.debug">

	<!-- Config -->
	<property file="../build/build.config" />
	
	<!-- Install on iOS -->
	<target name="install.ios.debug">
		<exec executable="${air.sdk}/bin/adt${bat.ext}" failonerror="true" dir=".">
            <arg line="-installApp"/>
            <arg line="-platform ios"/>
            <arg line="-package bin-debug/${name}Test.ipa"/>
        </exec>
	</target>
	
	<target name="install.ios">
		<exec executable="${air.sdk}/bin/adt${bat.ext}" failonerror="true" dir=".">
            <arg line="-installApp"/>
            <arg line="-platform ios"/>
            <arg line="-package bin/${name}Test.ipa"/>
        </exec>
	</target>

</project>